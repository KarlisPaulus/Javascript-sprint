function createPerson (firstName, lastName, age) {
    return "firstName: " + "'" + firstName + "'," + " lastName: " + "'" + lastName + "'," + " age: " + age ;
}

/*const person = createPerson('Max', 'Cool', 35)
console.log(person) */